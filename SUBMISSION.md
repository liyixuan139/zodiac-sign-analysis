# 🏆 WebMCP Challenge 投稿文档

> 提交前请把下面几处 `<占位>` 替换成实际链接。

---

## 项目信息

| 项 | 内容 |
|----|------|
| 项目名称 | 星语 · 星座分析（Xingyu Zodiac） |
| 一句话介绍 | 一个通过 WebMCP 让 AI 助手直接调用星座功能的趣味网站 |
| 部署链接 | `<填线上链接，如 https://xxx.vercel.app 或 Netlify 链接>` |
| 开源仓库 | `<填 GitHub 仓库链接>` |
| 演示视频 | `<填 3 分钟视频链接>` |

---

## 📝 文字说明（可直接粘贴到投稿表单）

### 中文版

星语是一个星座分析网站，用 WebMCP 把网站的四个核心能力直接暴露给了 AI 助手。
输入生日即可获得完整的星座档案——性格、爱情、事业、健康、幸运信息、当日运势与配对测试，
全部由同一份确定性算法生成，结果稳定可复现。

WebMCP 集成是本次参赛的核心：网站通过 `document.modelContext.registerTool()`
注册了 4 个只读工具（`list_zodiac_signs`、`analyze_birthday`、`daily_fortune`、
`pair_compatibility`），AI 助手可以像调用 API 一样直接使用网站功能，而无需模拟点击。
星座参数的识别对 AI 非常友好——英文 key（aries / leo）、中文名（白羊座）、英文名（Aries）、
符号（♈）都可以直接识别。

在 ChatGPT 桌面版中打开网站后，用户可以直接说：
「1996 年 8 月 15 日出生的人是什么星座，今天运势怎么样？」
AI 就会调用本站的 `analyze_birthday` 工具当场给出答案。

实现保持极简：`webmcp.js` 是纯前端文件、无任何外部依赖，普通浏览器访问时静默跳过，
完全不影响原有功能。全站共用一份数据模块（浏览器 / Node / 后端），共 12 星座 ×
完整档案 + 7 天 / 30 天运势趋势 + 配对测试 + 生日提醒。

### English version

Xingyu Zodiac is an astrology analysis website that exposes its core capabilities
to AI assistants through WebMCP. Enter a birthday and get a complete zodiac profile —
personality, love, career, health, lucky info, daily fortune and compatibility —
all generated by a deterministic algorithm so results are stable and reproducible.

The heart of this submission is the WebMCP integration: the site registers 4
read-only tools via `document.modelContext.registerTool()` — `list_zodiac_signs`,
`analyze_birthday`, `daily_fortune` and `pair_compatibility` — so an AI agent can
use the site's features like an API, without simulating clicks. Sign parameters are
agent-friendly: English keys (aries / leo), Chinese names (白羊座), English names
(Aries) or symbols (♈) are all accepted.

After opening the site in ChatGPT desktop, a user can simply ask:
"What zodiac sign is someone born on August 15, 1996, and how is their fortune today?"
The AI calls the `analyze_birthday` tool and answers immediately.

The implementation is intentionally minimal: `webmcp.js` is a dependency-free
frontend file that silently no-ops in normal browsers, so it never breaks existing
functionality. The whole site shares one data module across browser / Node /
backend, covering 12 signs with full profiles, 7/30-day fortune trends, pair
compatibility, and birthday reminders.

---

## ✨ 核心亮点（给评委）

1. **零依赖的 WebMCP 集成**：`webmcp.js` 只有约 200 行、纯浏览器 API、无需构建，任何静态托管即可运行。
2. **AI 友好参数设计**：星座可用 key / 中文 / 英文 / 符号四种写法，并内置 `list_zodiac_signs` 让 AI 自发现。
3. **优雅降级**：普通浏览器完全无感知；仅 AI 环境显示绿色徽章并注册工具。
4. **确定性算法**：同一天同一星座结果永远一致，演示可稳定复现。
5. **多形态复用**：同一份数据同时驱动网页、CLI、后端 API 与 WebMCP 工具。

---

## 🔧 如何本地复现

```bash
cd <项目目录>
npm install
npm start                 # 打开 http://localhost:3000
```

- 在 **ChatGPT 桌面版** 中打开该地址（或线上链接），页面顶部出现绿色徽章「🤖 AI 助手已连接」即表示工具注册成功。
- 直接对 AI 说（示例见下方视频脚本）。
- 普通 Chrome 可用 `chrome://flags/#enable-webmcp-testing` 开启后调试，或加入 Chrome 149+ Origin Trial。

---

## ✅ 提交前自检清单

- [ ] 代码已开源并提交 GitHub（仓库链接填入上方）
- [ ] 线上部署链接可公开访问（HTTPS）
- [ ] 3 分钟演示视频已录制并上传（脚本见 `DEMO-SCRIPT.md`）
- [ ] 视频、文字描述、可运行代码库均已按比赛页面要求提交
- [ ] 确认参赛地区在允许列表内
