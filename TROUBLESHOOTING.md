# 故障排查：部署链接打不开

> 记录 Claude Code 生成星座网站后，分享链接无法访问的问题。

---

## 一、问题描述

网站部署好后，把链接发给别人（或自己点开），页面一直**转圈、加载不出来**，浏览器提示：

> 响应时间过长，无法访问此页面

链接形如 `https://xxx.vercel.app`。

## 二、产生原因

不是网站坏了，也不是写错了代码，而是**网络通路被挡**：

- 部署用的平台是 **Vercel**，它生成的域名 `xxx.vercel.app` 在国内网络环境下会被屏蔽（解析到的服务器地址在国外被封禁的网段里）。
- 所以国内访问时，请求根本发不出去，一直等不到回应，就显示"响应时间过长"。

一句话总结：**链接没问题，是这个域名在国内访问不了。**

## 三、解决方法

换一个国内能正常访问的托管平台即可：

| 步骤 | 操作 |
|------|------|
| 1 | 改用 **Netlify** 部署（用 GitHub 账号直接登录，不需要另外注册 Netlify） |
| 2 | 部署后用账号**认领站点**，并在设置里**设为公开** |
| 3 | 得到新的 `https://xxx.netlify.app` 链接，国内即可正常打开 |

## 四、以后怎么避免

1. 面向国内用户，优先选 **Netlify / GitHub Pages** 这类域名（`netlify.app`、`github.io`），**别用 `vercel.app`**。
2. 部署完**先自己点开链接测一遍**，能正常打开再发给别人。

---

*当前项目线上链接：https://enchanting-cannoli-1e82f7.netlify.app（已解决 ✅）*
