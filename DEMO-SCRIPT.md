# 🎬 3 分钟演示视频脚本

> 总时长 **3:00**。用 OBS / 系统录屏 + 麦克风即可，无需剪辑技巧。
> 录制前先按 [SUBMISSION.md](SUBMISSION.md) 部署好线上链接，并确认 ChatGPT 桌面版能打开页面。

---

## 分镜总览

| 时间段 | 场景 | 时长 |
|--------|------|------|
| 0:00 – 0:30 | 开场：一句话介绍 + 网站功能快览 | 30s |
| 0:30 – 1:05 | 什么是 WebMCP + 注册的 7 个工具 | 35s |
| 1:05 – 2:25 | 核心演示：ChatGPT 调用 4 个工具（含生日设置爆点） | 80s |
| 2:25 – 3:00 | 优雅降级 + 结尾总结 | 35s |

---

## 0:00 – 0:35 开场 · 网站功能快览

**画面**：浏览器打开站点首页，切到「星座分析」标签，输入生日 1996-08-15 点「✦ 开始分析」。
展示雷达图、今日运势、性格/爱情/事业/健康卡片，快速滑到运势趋势图、配对测试、生日提醒。

**旁白**：
> 这是一个星座分析网站。输入生日，它就能给出完整的星座档案——
> 性格、爱情、事业、健康、幸运信息，还有 7 天 / 30 天的运势趋势和配对测试。
> 它跑起来只需要一个静态网页，没有任何后端依赖。

---

## 0:30 – 1:05 什么是 WebMCP

**画面**：切到代码编辑器，打开 `webmcp.js`，高亮 `registerTool` 片段，依次展示 7 个工具名；
再切回页面，顶部出现绿色徽章「🤖 AI 助手已连接」。

**旁白**：
> 这次参赛的关键，是我给这个网站加上了 WebMCP——
> 让 AI 助手不用模拟点击，而是直接调用网站暴露的工具。
> 这里注册了 7 个工具：4 个只读分析（列出星座、生日分析、每日运势、配对测试），
> 还有 3 个能真正操作网站的动作工具——设置生日、读取生日、删除生日。
> 只要在支持 WebMCP 的浏览器里打开页面，顶部就会出现这个绿色的「AI 已连接」徽章。

---

## 1:05 – 2:25 核心演示 · ChatGPT 直接调用（约 4 个小段）

> 以下四段在 ChatGPT 桌面版中连续演示，每段先打字幕中的提示词，AI 调用工具后把结果亮给镜头看。

### ① 生日 → 星座分析（约 18s）

**画面**：ChatGPT 桌面版打开本站，输入：
```
帮我看看 1996 年 8 月 15 日出生的人是什么星座，今天运势怎么样？
```
**旁白**：
> 我直接问 ChatGPT——「1996 年 8 月 15 日出生的人是什么星座，今天运势怎么样？」
> 它调用了 `analyze_birthday` 工具，马上给出狮子座，以及今天的运势指数和解读。

### ② 生日设置 · 全片爆点（约 30s）

**画面**：继续输入（AI 会自动填今天的日期，页面当场弹出彩带庆祝动画 + 生日横幅 + 系统通知）：
```
帮我把生日设置成今天，我想看庆祝效果。
```
**旁白**：
> 这里是最关键的一段——我让 ChatGPT「帮我把生日设置成今天」。
> 它调用了 `set_birthday` 这个有状态的动作工具，把生日写进了网站。
> 你看，页面当场弹出了彩带和生日横幅——因为今天真的是我的生日。
> 这不再是只读查询，而是 AI 真的在帮我操作这个网站。

### ③ 读取生日 · 状态闭环（约 12s）

**画面**：继续问：
```
那我是哪个星座？生日提醒设好了吗？
```
**旁白**：
> 我再问一句「我是哪个星座」，它调用 `get_birthday` 读取刚才保存的生日，
> 告诉我星座，并确认生日提醒已经设好。保存的状态是可读回的，形成完整闭环。

### ④ 配对测试（约 15s）

**画面**：继续问：
```
白羊座和狮子座配吗？给我个缘分指数。
```
**旁白**：
> 最后快速测一下「白羊座和狮子座配不配」——`pair_compatibility` 给出 89 分和相处建议。
> 星座用中文、英文、符号都能识别，工具自带容错，AI 很难用错。

---

## 2:25 – 3:00 优雅降级 + 结尾

**画面**：切到普通 Chrome（无 WebMCP），演示页面一切如常、没有徽章；切回编辑器显示 `webmcp.js` 只有约 260 行。

**旁白**：
> 普通浏览器访问时，`webmcp.js` 检测不到 `document.modelContext` 就静默跳过——
> 页面功能完全不受影响，零成本升级。
> 整个 WebMCP 集成就是一个约 260 行、无任何外部依赖的纯前端文件，
> 任意静态托管部署即可生效。欢迎把代码部署到你的浏览器里试试看。谢谢！

---

## 🎯 推荐的四条演示提示词（可直接复制）

1. `帮我看看 1996 年 8 月 15 日出生的人是什么星座，今天运势怎么样？`
2. `帮我把生日设置成今天，我想看庆祝效果。`（全片爆点：页面当场弹彩带庆祝）
3. `那我是哪个星座？生日提醒设好了吗？`
4. `白羊座和狮子座配吗？给我个缘分指数。`

---

## ⚠️ 录制注意

- 录屏前先确认：站点已部署、HTTPS、ChatGPT 桌面版能打开且出现绿色徽章。
- 如果 `analyze_birthday` 弹「请选择浏览器内打开」，用 ChatGPT 桌面版内置浏览器打开站点（而不是外部浏览器）。
- 提示词结果含中文，直接展示即可，评委可理解。
- 全程不需要展示代码细节，重点展示「AI 与网站的工具交互」。
- 留 3–5 秒缓冲再开始/结束，方便剪辑。
